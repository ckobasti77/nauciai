# V4 - dnevnik implementacije

Svaki korak dopisuje svoj red na kraj. Ne brisati ranije redove.

Korak | Status | Sta je uradjeno | Fajlovi
--- | --- | --- | ---
N1 | gotovo | Nova singleton tabela `platformSettings` (kontakt, mreze, cene, pravni podaci) sa javnim `get` upitom i admin-only `update` mutacijom koja validira e-adresu, E.164 telefon i https URL mreze sa ocekivanog domena. Novi admin ekran /app/admin/settings sa cetiri kartice (cuvanje po kartici, optimisticki prikaz, toast, napomena gde se polje prikazuje) i nova stavka „Opste informacije“ u admin navigaciji. Sekcija cena na landingu vise ne cita `lib/pricing.ts` direktno nego `platformSettings` kroz `resolveSettings`; `lib/pricing.ts` je od sada samo rezerva. | `convex/schema.ts`, `convex/platformSettings.ts`, `convex/platformSettings.test.ts`, `lib/platform-settings.ts`, `lib/platform-settings.test.ts`, `lib/pricing.ts`, `lib/convex-http.ts`, `lib/sidebar-contexts.ts`, `lib/sidebar-contexts.test.ts`, `components/app/admin-platform-settings.tsx`, `components/marketing/marketing-page.tsx`, `app/[locale]/app/admin/settings/page.tsx`, `app/[locale]/(marketing)/page.tsx`
N2 | gotovo | Navbar javnih strana izdvojen u `components/marketing/public-header.tsx` i podignut u `(marketing)/layout.tsx`, pa isti navbar stoji na svim javnim stranama; landing, studio, kursevi, zajednica, prijava, reset i pravne strane su prestale da renderuju svoje zaglavlje, a `.public-shell` rezervise visinu lebdeceg navbara tamo gde nema full-bleed heroja. Sadrzaj navbara je mreza od tri jednake kolone (logo levo, linkovi tacno na sredini, kontrole desno; ispod `md` sredina se skriva i vraca se `justify-between`), a jezicki prekidac sada ostaje na istoj strani. Novi `data-over-light` na headeru drzi logo, linkove i ikonice u mastilu dok navbar lebdi iznad svetlog heroja i u tamnoj temi; posle skrola boje se vracaju na temu. Dropdown naloga je izgubio odvojeni gornji blok - prvi red liste je sada link ka `/app/profile` sa profilnom slikom u krugu velicine ostalih ikonica, imenom i @korisnickim imenom u dva reda i ulogom kao Badge; paddinzi su smanjeni. | `components/marketing/public-header.tsx`, `app/[locale]/(marketing)/layout.tsx`, `components/marketing/marketing-page.tsx`, `components/marketing/account-menu.tsx`, `components/marketing/language-toggle.tsx`, `components/marketing/legal-page.tsx`, `app/globals.css`, `app/[locale]/(marketing)/studio/page.tsx`, `app/[locale]/(marketing)/courses/page.tsx`, `app/[locale]/(marketing)/courses/[courseSlug]/page.tsx`, `app/[locale]/(marketing)/community/page.tsx`, `app/[locale]/(marketing)/community/[slug]/page.tsx`, `app/[locale]/(marketing)/sign-in/page.tsx`, `app/[locale]/(marketing)/reset-password/page.tsx`
N3 | gotovo | Hero kompozicija se na sirokim/niskim ekranima (gde je vizual fit-po-visini) vise ne kaci za desnu ivicu viewport-a nego za isti centrirani `max-w-7xl` kontejner u kom je i tekst: kad je vizual uzi od kontejnera desna ivica mu sedne na desnu ivicu kontejnera, a kad je siri centrira se - u oba slucaja je krem visak jednak levo i desno (izmereno 122,8/122,8 na 1536x695 i 310,5/307,6 na 2560x1080). Sloj 3D kartica nosi istu klasu pa se pomera zajedno sa videom; crveni quad lista je proveren na 1920x1080, 2560x1440, 1440x900 i 1536x695 i svuda lezi tacno na svesci. Hero CTA su zamenjena: levo zuto „Besplatan video" (PlayCircle) vodi na besplatan video kursa, desno paper „Otvori Studio" (Sparkles) vodi pravo u alat - prijavljen u /app/studio, gost u /studio kad je javni Studio upaljen, inace na prijavu sa `next`. Za to je dodat javan Convex upit `studio:isPublicEnabled`. | `app/globals.css`, `components/marketing/marketing-page.tsx`, `app/[locale]/(marketing)/page.tsx`, `lib/i18n.ts`, `lib/convex-http.ts`, `convex/studio.ts`
N4 | gotovo | Podnozje javnih strana je gusce: paddinzi sekcije smanjeni skoro na pola, linkovi u kolonama vise nemaju 44px klik-metu na desktopu (samo `pointer-coarse`), naslovi kolona su `type-eyebrow`, kolone su poravnate po vrhu, a copyright/jezik/tema su spojeni u jedan red od 640px na gore. Jezicki prekidac u podnozju i u samostalnom Studio shell-u sada koristi zajednicku `LanguageToggle` komponentu (istu kao u navbaru) umesto sopstvenih tekstualnih/pill varijanti; talas iznad podnozja je vec bio jedna tamna linija (v3) pa nije menjan. | `components/marketing/site-footer.tsx`, `components/studio/studio-shell.tsx`, `lib/studio-messages.ts`
N5 | gotovo | Nova leva kontakt traka (`components/marketing/contact-rail.tsx`), fixed na levoj ivici na istoj visini kao `ScrollToTop` (z-40, ispod dropdown-a i modala), skrivena ispod 640px jer isti kontakti vec postoje u podnozju. Redosled odozdo nagore: telefon (`tel:`), email (`mailto:`), pa dugme "Drustvene mreze" koje otvara kolonu ikonica (Instagram/Facebook/TikTok/YouTube/Threads, `target=_blank` + `noopener noreferrer`) samo za mreze sa popunjenim URL-om - polje bez vrednosti ne renderuje dugme. Ulaz ikonica ide odozdo nagore uz stagger 40ms (GSAP, ease iz `lib/motion-contract`), izlaz obrnuto i brze (140ms), bez stagger-a pri `prefers-reduced-motion`. Zatvara se na ponovni klik, klik van trake, Escape (vraca fokus na dugme) i skrol; dok je otvoreno Tab kruzi samo kroz dugme i ikonice (`tabTrapAction` iz `lib/focus-trap.ts`, isti racun kao modal fokus zamka). Podaci stizu server-side u `(marketing)/layout.tsx` preko `resolveSettings` (N1), pa je traka renderovana samo na javnim stranama (isti wrapper kao `PublicHeader`/`SiteFooter`) - ne u top-level `AppProviders` gde bi se preklapala sa sidebar-om na `/app` dashboard-u; to odstupanje od bukvalnog teksta zadatka je zabelezeno u izvestaju. | `components/marketing/contact-rail.tsx`, `app/[locale]/(marketing)/layout.tsx`, `lib/i18n.ts`

N6 | gotovo | Nova javna strana pretplate na dva URL-a sa prevedenim segmentom (sr /pretplata, en /pricing, jedina tacka istine je novi `pricingPath` iz `lib/i18n`; suprotna kombinacija se preusmerava 308 kroz `redirects()` u `next.config.ts`). Strana ide istim sistemom naizmenicnih povrsina i talasa kao landing (A-B-A-B-A, poslednja A zbog talasa iznad podnozja): naslov sa markerom, kartice Basic/Premium sa duzim spiskom stavki, tabela razlika sa sest mogucnosti koja se ispod md pretvara u dve kartice jedna ispod druge (bez horizontalnog skrola), pojedinacni kursevi sa jednokratnom cenom i CTA `Pogledaj kurs`, pet pitanja o naplati sa istom animacijom otvaranja kao FAQ na landingu, i zavrsni CTA. Kartice planova su izdvojene u `components/marketing/plan-cards.tsx` pa landing i nova strana dele identicne kartice (i robote sa istim faznim pomakom); citanje cena i kredita izdvojeno u `lib/pricing-data.ts`. Cene planova i dalje dolaze iz `platformSettings` kroz `resolveSettings`, a jednokratna cena kursa iz nove rezerve `PRICING.course` jer `platformSettings` jos nema polje za nju. Link `Pretplata` u navbaru vodi na novu stranu umesto na #pricing, u podnozju je dodat isti link, prekidac jezika na toj strani prelazi pravo na prevedeni segment, sekcija #pricing na landingu je dobila dugme `Uporedi planove detaljno`, a obe varijante strane su dodate u sitemap. | `app/[locale]/(marketing)/pretplata/page.tsx`, `app/[locale]/(marketing)/pricing/page.tsx`, `components/marketing/pricing-page.tsx`, `components/marketing/plan-cards.tsx`, `components/marketing/marketing-page.tsx`, `components/marketing/public-header.tsx`, `components/marketing/site-footer.tsx`, `lib/i18n.ts`, `lib/pricing.ts`, `lib/pricing-data.ts`, `lib/pricing-page.test.ts`, `app/[locale]/(marketing)/page.tsx`, `app/sitemap.ts`, `next.config.ts`
N7 | gotovo | Nova deljena komponenta `components/marketing/page-hero.tsx`: full-bleed krem hero javnih podstrana u istom jeziku kao landing (tekst levo u centriranom max-w-7xl kontejneru, vizual desno, maskirane ivice, mastilo linija umesto talasa ispod), ali nizi - `min-height: 62svh` kroz novu `.page-hero` klasu, pa sekcija poraste kad je kopija visa umesto da je sece. Centriranje je isto pravilo kao N3 ali bez ijednog media upita na odnos ekrana: vizual je `contain` (max-w-full/max-h-full) u kutiji koja je isti centrirani kontejner i poravnat je uz njenu desnu ivicu - izmereno na 1920x900, desna ivica vizuala tacno na desnoj ivici kontejnera i krem visak 314.8 px levo / 315 px desno. Medij je opcion i degradira graciozno preko novog `lib/public-media.ts` (`existingPublicPath`): poster + mp4 daju video sa posterom, samo poster daje mirnu sliku, a kad nema nijednog fajla ostaje samo krem podloga - nikad polomljen `<video>` ni prazan pravougaonik (provereno oba puta). Ispod `lg` se vizual ne prikazuje jer su svi hero assetovi crtani sa praznom levom trecinom za tekst, pa bi na uskom ekranu tekst pao na ilustrovani deo. Primenjeno na /courses (novi naslov `Dva kursa, jedan gotov rad`, CTA `Pogledaj kurseve` na sidro liste i `Uporedi planove` na /pretplata), /community (`Uci javno, napreduj brze`, CTA `Udji u zajednicu` i `Pogledaj diskusije`; dugme `Postavi pitanje` je izaslo iz starog panela naslova i stoji iznad liste) i /studio (isti tekst, CTA i bonus red, samo je hero presao na komponentu i dobio centriranje). Obe liste su prebacene na `max-w-7xl` sa `px-4 sm:px-6 lg:px-8`, a /courses i /community su dodate u `lightHeroPaths` pa navbar preko njih lebdi kao na landingu. | `components/marketing/page-hero.tsx`, `lib/public-media.ts`, `app/globals.css`, `app/[locale]/(marketing)/courses/page.tsx`, `app/[locale]/(marketing)/community/page.tsx`, `app/[locale]/(marketing)/studio/page.tsx`, `components/marketing/public-header.tsx`, `lib/i18n.ts`
N8 | gotovo | Sidebar dashboarda ima sada tacno dva stanja i nijednu drugu prelomnu tacku. Dugme za kolaps je samo ikonica u boji mastila (bez okvira, pozadine i senke; hover je iskljucivo podloga na 6% mastila), stoji na istom mestu i kad je sidebar zatvoren, a pri promeni stanja se ikonica okrene za 180 stepeni u 200ms. Sirine su izmerene i upisane kao tokeni u `app/globals.css`: `--sidebar-w` 246px (2 okvir + 16 padding + 147 logotip h-11 + 16 razmak + 48 dugme + 16 padding — najuza sirina na kojoj dugme ne prelazi preko logotipa) i `--sidebar-w-collapsed` 82px (48 krug ikonice + 2x16 padding + 2 okvir). Na toj sirini svaki nav link staje u jedan red bez preloma i bez tri tackice u oba jezika (mereno u Nunito 800/14px: najduzi sr `Opste informacije` 117px i `Chat sigurnost` 98px, najduzi en `Notifications` 85px + 8 + 28 za badge `99+`; kolona za tekst je 152px). Slobodno prevlacenje sirine (280–420px) je uklonjeno jer se kosi sa „samo dva stanja": kolacic pamti samo `collapsed`, a ivica sidebara je sarka koja preskace izmedju istih sirina kao i klik na dugme, pa je prelaz identican. Ikonice se vise ne pomeraju ni ne menjaju velicinu — u oba stanja su 20px i na istom x-u (izmereno 39,6 / 40,2 px), jer sedi u koloni sirine kruga umanjenoj za okvir reda, a tekst je zaseban element DESNO od nje sa FIKSNOM sirinom (`--sidebar-label-w`) koji se nikad ne skuplja nego ga red odseca. Nav pocinje na y=96 u oba stanja (rail je izgubio emblem i crtu koje su ga gurale nize, a red zaglavlja je fiksiran na 48px jer je inline line-box oko logotipa dodavao 3px), pa nema vertikalnog pomeranja. Animacija: sirina 260ms sa `--ease-studio-out`, tekst opacity 0→1 i translateX -4px→0 u 160ms sa kasnjenjem 80ms pri otvaranju i bez kasnjenja pri zatvaranju; rail sloj se pojavljuje tek kad se sirina skupi, a pri otvaranju se sklanja jedan frejm pre nego sto sirina krene (bez tog frejma tranzicija teksta nema odakle da krene i tekst bane odjednom). Glavni sadrzaj prati istu krivu jer je flex sused, bez horizontalnog scrollbar-a (provereno kroz ceo prelaz). `prefers-reduced-motion` daje trenutnu promenu. Kartica profila je pratila suzavanje: avatar je 48px na istoj x osi kao ikonice, a znacka uloge je presla u drugi red uz @username da ime ostane citljivo. Provereno na 1920x1080, 1536x695 i 1280x720, u sr i en, u obe teme, u mobilnoj fioci (gde `data-collapsed` nema efekta) i kroz 3 kadra prelaza. | `app/globals.css`, `components/app/app-sidebar.tsx`, `components/app/app-sidebar-context.tsx`, `lib/app-sidebar-preferences.ts`, `lib/app-sidebar-preferences.test.ts`
N9 | gotovo | Popup naloga u dashboardu je sada ista stvar kao dropdown naloga na javnim stranama: nova deljena komponenta `components/app/app-account-menu.tsx` nosi isti radius (16px), istu tvrdu senku (8px) i isti repic okrenut ka dugmetu koje je otvara, a redosled je profil -> linkovi -> alatke -> odjava. Prvi red JE profil: slika naloga u krugu umesto ikonice, ime i @korisnicko ime u dva reda, oznaka uloge desno, i vodi na /app/profile (upozorenja o nalogu ostaju kao zuta tacka na avataru, isto kao javna verzija). Ispod su Podesavanja (/app/profile#account-settings, sidro sekcije naloga, da ne bude dva ista linka) i Pretplata, plus Unapredi plan kad plan to nudi (ostao je jer je u prosirenom sidebaru jedina tacka nadogradnje; presao je sa golog #10b981 na zutu iz palete). Red alatki nosi kredite, zvuk, temu i prekidac jezika iz N4 - `LanguageToggle` sa `href` na ISTU stranu na drugom jeziku, po istom pravilu kao javni navbar. Na dnu je Odjavi se preko cele sirine u punom mastilu. Isti panel koristi i prosireni sidebar (repic dole) i rail (repic levo), pa skupljeno i prosireno stanje vise ne nude dva razlicita menija; `CreditsBalancePill`, `SidebarRoleBadge`, `resolvePlan` i `planOffersUpgrade` su presli u novi modul (bez kruzenja uvoza), a `SoundToggle` je dobio `iconOnly` da red alatki bude jedan oblik. ISPADI NA UGLOVIMA: uzrok je uvek isti - dete koje crta (pozadina ili gornji/donji okvir) naleze na ivicu zaobljenog roditelja koji ga ne secka, ili dete ima veci radius od roditelja. Umesto zakrpe po ekranu, pravilo je pretoceno u `scripts/check-corners.mjs` (`npm run check:corners`) koji parsira JSX stablo svih .tsx fajlova u `components/` i `app/` i pada kad naidje na takav par; skripta je proverena tako sto vraceni bag ponovo obara gejt. Nadjena i popravljena tri mesta: zaglavlje panela Razgovori u porukama (`chat-inbox.tsx` - najuocljiviji rogalj, panel je dobio overflow-hidden), skelet ucionice koji preslikava panel koji u pravoj verziji secka (`classroom-hub.tsx`), i biracka fioka modela u Studiju gde je zaglavlje uzimalo `rounded-t-[inherit]` (spoljni radius 16px iako sedi unutar okvira od 2px, pa mu je ugao virio 2px izvan roditeljeve krive) - sada roditelj secka, a dete nema svoj radius. Ostatak platforme (dashboard, zajednica, admin, ostatak Studija i ucionice) je cist, sto skripta i tvrdi. OBAVESTENJA: popup je suzen sa 22rem na 18rem, i to izmereno - najduzi kanal U aplikaciji je 63,9px (Nunito 900/12px) + 13,6 padding/okvir = 78px po koloni, tri kolone + razmaci + paddinzi = 283px; tri kanala sada stoje u mrezi od tri jednake kolone pa popunjavaju red do kraja (izmereno 79,7px po koloni, bez skracivanja teksta u sr i en), a kartice po tipu su kompaktnije (p-2.5 -> p-2, razmaci 2 -> 1.5). Glavno zvono je dodato u zaglavlje popupa i NIJE zaseban prekidac nego pogled na iste redove: `lib/notification-preferences.ts` racuna stanje sve-ugaseno / delimicno / sve-upaljeno, gasenje gasi sve kanale svih tipova (uz snimak u localStorage), paljenje vraca zapamceno stanje (bez snimka pali sve, da paljenje nikad ne ostavi tisinu), a bilo koji upaljen kanal drzi zvono upaljenim; stanje nosi i `aria-pressed=mixed`. Provereno u browseru: gasenje zvona -> 0/15 kanala i Sve iskljuceno, paljenje -> 15/15, jedan kanal ugasen -> mixed i Delimicno ukljuceno. | `components/app/app-account-menu.tsx`, `components/app/app-sidebar.tsx`, `components/app/sound-toggle.tsx`, `components/app/chat/chat-inbox.tsx`, `components/app/classroom-hub.tsx`, `components/studio/studio-composer.tsx`, `lib/notification-preferences.ts`, `lib/notification-preferences.test.ts`, `lib/i18n.ts`, `scripts/check-corners.mjs`, `package.json`
N10 | gotovo | Bocna navigacija Ucionice vise nema karticu aktivnog kursa sa strelicom nego PUTANJU KONTEKSTA: tri nivoa jedan ispod drugog (SMER > KURS > LEKCIJA), svaki uvucen za korak dublje i povezan tankom vertikalnom linijom u boji `--line` sa crticom do svakog reda; nivo LEKCIJA postoji samo dok je lekcija otvorena. Klik na nivo otvara popover sa listom stavki TOG nivoa (smerovi / kursevi u tom smeru / lekcije u tom kursu) i prebacuje kontekst; lista je u toku ispod bloka jer se isti blok renderuje i u rail flyout-u koji sam skroluje, pa bi apsolutni sloj tamo bio odsecen. Oznaka stanja (AKTIVAN / ZAKLJUCAN / SKICA) stoji uz NADNASLOV, ne uz naziv - izmereno u koloni od ~150px, pored naziva je jela dve trecine reda i kurs je ostajao "Video a..."; uz admin "+" (koji po zadatku stoji pored nivoa KURS) oznaka se prelama u sledeci red umesto da se sece u "AC...". Skupljeni sidebar svodi blok na jednu ikonicu koja otvara isti popover; bez ijednog upisanog kursa oba stanja daju jedan red "Izaberi smer" ka Ucionici (uslov je stvarno vlasnistvo, jer `currentCourse` pada na `courses[0]` i za neupisanog studenta). Ucionica je prestala da bude niz panela iste boje: cetiri PUNE trake koje se smenjuju po `lib/surface.ts` i razdvajaju `SectionWave`-om, isto kao javne strane - A "Nastavi gde si stao" (velik medij levo, naziv lekcije, napredak i CTA desno), B smerovi kao horizontalne trake sa minijaturama kurseva, C katalog u dve kolone, D sledece lekcije. Kartice dobijaju nivo svoje trake i uzimaju SUPROTNU povrsinu - bez toga bi `bg-paper-strong` bio identican traci u jednoj od dve teme (svetla: paper-strong = surface-a; tamna: paper-strong = surface-b). Medij u zoni A i na karticama zone C je besavna petlja kursa iz `public/images/landing/course-*-loop.mp4` preko postojeceg `LoopVideo` (mp4 prvi zbog LiteSpeed-a): provereno u pregledacu - tri videa, samo onaj u kadru ima `preload=metadata` i svira, oba iz zone C pauziraju kad izadju iz kadra, a `prefers-reduced-motion` daje 0 videa i tri postera. Cela kartica kursa je klik meta (sloj linka ispod sadrzaja, dugmad Otkljucaj/Otvori kurs/Zajednica iznad njega - hit-test potvrdio oba), pa je link "Detalji" uklonjen jer je bio druga destinacija ka istom mestu. Minijature u zoni B ostaju mirne slike namerno (red od pet videa koji se sami vrte nije ritam nego buka). | `components/app/app-sidebar.tsx`, `components/app/classroom-hub.tsx`, `components/app/course-catalog-card.tsx`, `components/app/dashboard-content.tsx`, `lib/course-catalog.ts`, `lib/course-catalog.test.ts`
